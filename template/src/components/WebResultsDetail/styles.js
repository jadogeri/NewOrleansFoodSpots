import { StyleSheet } from "react-native";

const styles = StyleSheet.create({

    image: {
        height: 400,
        width: 450,
        borderRadius: 4,
        marginLeft: 10
    },
    name: {
        fontWeight: "bold",
        fontSize: 20
    }
})

export default styles;